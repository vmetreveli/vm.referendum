{
  "$schema": "https://schema.railpack.com",
  "provider": "dotnet",
  "steps": {
    "build": {
      "commands": [
        "dotnet publish src/referendum.api/referendum.api.csproj -c Release -o out"
      ]
    }
  },
  "deploy": {
    "startCommand": "dotnet out/referendum.api.dll"
  }
}